import Modal from '../UI/Modal/Modal';
import './ScheduleForm.scss';

interface Props {}

const ScheduleForm = ({}) => {
  return (
    <Modal>
      <form></form>
    </Modal>
  );
};

export default ScheduleForm;
